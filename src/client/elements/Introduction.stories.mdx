import { Meta } from '@storybook/addon-docs';

<Meta title="App/Introduction" />

# UI Components

UI elements are testable in the panel on the left. All the tests are defined as `*.stories.ts` files.